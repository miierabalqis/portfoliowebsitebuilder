const ExperienceDurationSection = ({startDate, endDate}) => {
    return (
        <div>
            <p>
                {startDate} - {endDate}
            </p>
        </div>
    );
};

export default ExperienceDurationSection;
