// components/sections/EducationSection.js
const DurationSection = ({startDate, endDate}) => {
    return (
        <div>
            <p>
                {startDate} - {endDate}
            </p>
        </div>
    );
};

export default DurationSection;
