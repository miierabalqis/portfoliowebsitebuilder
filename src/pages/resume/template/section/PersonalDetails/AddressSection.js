const AddressSection = ({personalDetail}) => {
    return (
        <div>
            <div>
                <h1>{personalDetail?.address}</h1>
            </div>
        </div>
    );
};

export default AddressSection;
