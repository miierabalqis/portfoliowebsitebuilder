const NameSection = ({personalDetail}) => {
    return (
        <div>
            <div>
                <h1>{personalDetail?.name}</h1>
            </div>
        </div>
    );
};

export default NameSection;
